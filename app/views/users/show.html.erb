<% content_for(:title, "Profile Page") %>

<%= @user.name %>, <%= @user.email %>
<br>
<% if current_user.admin? %>
  <%= @user.tasks.each do |task| %>
    <%= task.title %><br>
    <%= task.start %><br>
    <%= task.endtime %><br>
    <%= task.priority %><br>
    <%= task.status %><br>
  <% end %>
<% end %>